export enum ScoreType {
    connect,
}

export enum KPreviewZoom {
    NORMAL = "normal",
    SMALL = "small",
    LARGE = "large",
}

export enum KPreviewThemeMode {
    LIGHT = "light",
    DARK = "dark",
    BLURED = "blured",
}
